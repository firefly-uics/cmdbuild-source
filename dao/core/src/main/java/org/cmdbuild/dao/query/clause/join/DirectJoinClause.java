package org.cmdbuild.dao.query.clause.join;

public class DirectJoinClause {

	/**
	 * Store here the informations that are needed for a join with another class
	 * without a domain.
	 * 1) left join or simple join?
	 * 2) target class
	 * 3) target class alias
	 * 4) source class (that is the from class)
	 * 6) condition for join (ON "target_class_alias"."attribute" = "source_class_alias"."attribute")  
	 * 5) attributes with respective aliases to add to select  
	 */

}
